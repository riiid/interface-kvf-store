syntax = "proto3";

package riiid.kvf;

message ConvertPlan {
    repeated ConvertPlanEntry entries = 1;
}

message ConvertPlanEntry {
    oneof value {
        Convert convert = 1;
        Upcast upcast = 2;
    }
    message Convert {
        string format_id = 1;
    }
    message Upcast {
        string format_id = 1;
    }
}
